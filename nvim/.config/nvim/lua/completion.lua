-- Stub to prevent vim-dadbod-completion from erroring when it checks for
-- the legacy completion.nvim plugin (require('completion').addCompletionSource).
-- This config uses blink.cmp instead.
return {
	addCompletionSource = function() end,
}
